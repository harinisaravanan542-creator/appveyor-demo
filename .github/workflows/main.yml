name: CI Demo

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Run a script
      run: |
        echo "Building project..."
        mkdir output
        echo "Hello from CI/CD" > output/result.txt
