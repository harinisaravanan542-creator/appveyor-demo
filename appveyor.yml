image: Visual Studio 2022

# We will skip the real 'build' step for this demo 
# and just create a 'dummy' artifact to show how it works.
build_script:
  - echo "Building the project..."
  - mkdir build_output
  - echo "This is a demo file generated by AppVeyor" > build_output\readme.txt
  - echo "Project Version 1.0" > build_output\version.txt

# This is the "Magic" part that your instructors will want to see
artifacts:
  - path: build_output
    name: Demo_Build_Package
